def rule1(value)
  if ($value >= 50)
      2
    else
      1

def rule2(value)
  if ($value<10)
    1
  else
    if ($value>10 and $value<20)
     2
    else
     3

{
  "ColA" : rule2(.ColA),

  "ColB" : rule1(.ColB),

  "ColC" : rule1(.ColC),

  "ColD" : rule2(.ColD),

  * : .
}
